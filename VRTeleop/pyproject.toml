[project]
name = "vrteleop"
version = "0.1.0"
description = "Godot OpenXR to SO-101 teleoperation bridge"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
vrteleop-bridge = "vrteleop_bridge.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
