# ros2_ws に入ったら ROS 2 開発環境を自動で有効化する.
# ルート flake の devShells.<system>.ros を読み込む.
#   - Linux: native ROS 環境 (ros2, colcon) + podman
#   - macOS: podman (+ qemu) のみ. 実機/sim は ./podman/run.sh を使う.
use flake "$(git rev-parse --show-toplevel)#ros"

# colcon build 済みなら, ビルドしたワークスペースを overlay source する (Linux).
# cd で抜けると direnv が環境を巻き戻すのでクリーン.
watch_file install/setup.bash
if [[ -f install/setup.bash ]]; then
  source install/setup.bash
fi
