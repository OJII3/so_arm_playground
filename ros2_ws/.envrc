# ros2_ws に入ったら ROS 2 開発環境を自動で有効化する.
# Linux: nix-ros-overlay によるネイティブ ROS 2 環境.
# macOS: podman + XQuartz (コンテナ経由).
use flake "$(git rev-parse --show-toplevel)#ros"

# (Linux) colcon build 済みなら, ビルドしたワークスペースを overlay source する.
watch_file install/setup.bash
if [[ -f install/setup.bash ]]; then
  source install/setup.bash
fi
